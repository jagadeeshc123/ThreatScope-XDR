import { Trash2 } from 'lucide-react';
import type { ApiBusinessFlowStep, ApiEndpoint } from '../../../types';

export function FlowStepCard({ step, endpoint, onDelete }: { step: ApiBusinessFlowStep; endpoint?: ApiEndpoint; onDelete?: () => void }) {
  return <article className="relative border-l-2 border-indigo-400 pl-5"><span className="absolute -left-3 top-0 flex h-6 w-6 items-center justify-center rounded-full bg-indigo-500 text-xs font-bold text-white">{step.step_order}</span><div className="rounded-md border border-border bg-background/50 p-4"><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold">{step.action_name}</h3><p className="mt-1 font-mono text-xs text-indigo-200">{endpoint ? `${endpoint.method} ${endpoint.path}` : 'No endpoint linked'}</p></div>{onDelete && <button onClick={onDelete} title="Delete step" className="rounded-md p-2 text-muted-foreground hover:bg-red-500/10 hover:text-red-200"><Trash2 className="h-4 w-4" /></button>}</div><div className="mt-3 grid gap-2 text-xs text-muted-foreground sm:grid-cols-2"><p><strong className="text-foreground">Actor:</strong> {step.expected_actor_role || 'Not documented'}</p><p><strong className="text-foreground">Sensitive:</strong> {step.sensitive_operation ? 'Yes' : 'No'}</p><p><strong className="text-foreground">Prerequisite:</strong> {step.prerequisite_description || 'Not documented'}</p><p><strong className="text-foreground">State:</strong> {step.expected_state_before || '?'} to {step.expected_state_after || '?'}</p></div></div></article>;
}
