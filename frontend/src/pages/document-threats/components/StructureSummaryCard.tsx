import type{DocumentAnalysis}from'../../../types';export function StructureSummaryCard({item}:{item:DocumentAnalysis}){const rows=[['JavaScript',item.has_javascript],['OpenAction',item.has_open_action],['Additional actions',item.has_additional_actions],['Launch action',item.has_launch_action],['AcroForm',item.has_acroform],['XFA',item.has_xfa],['Embedded files',item.has_embedded_files],['External URIs',item.has_external_uris]];return <div className="grid gap-2 sm:grid-cols-2">{rows.map(([label,value])=><div key={String(label)} className="flex justify-between border-b border-border py-2 text-sm"><span>{label}</span><b>{value?'Detected':'Not observed'}</b></div>)}</div>}
