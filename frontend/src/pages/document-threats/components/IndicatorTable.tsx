import{toast}from'sonner';import type{DocumentIndicator}from'../../../types';export function IndicatorTable({items}:{items:DocumentIndicator[]}){return <div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead><tr className="border-b border-border"><th className="p-3">Type</th><th>Sanitized value</th><th>Context</th><th/></tr></thead><tbody>{items.map(i=><tr key={i.id} className="border-b border-border"><td className="p-3">{i.indicator_type}</td><td className="max-w-sm break-all font-mono text-xs">{i.display_value_redacted}</td><td>{i.context}</td><td><button onClick={()=>void navigator.clipboard.writeText(i.display_value_redacted).then(()=>toast.success('Copied sanitized text.'))} className="text-primary">Copy</button></td></tr>)}</tbody></table></div>}
