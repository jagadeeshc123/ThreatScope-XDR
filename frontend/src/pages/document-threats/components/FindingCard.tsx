import type{DocumentFinding}from'../../../types';export function FindingCard({finding}:{finding:DocumentFinding}){return <article className="rounded-lg border border-border bg-background/50 p-4"><div className="flex justify-between gap-3"><strong>{finding.rule_code} · {finding.title}</strong><span className="text-xs font-bold uppercase text-amber-300">{finding.severity}</span></div><p className="mt-2 text-sm text-muted-foreground">{finding.description}</p><p className="mt-3 text-sm"><b>Evidence:</b> <span className="text-muted-foreground">{finding.evidence_summary}</span></p><p className="mt-2 text-sm"><b>Remediation:</b> <span className="text-muted-foreground">{finding.remediation}</span></p></article>}
