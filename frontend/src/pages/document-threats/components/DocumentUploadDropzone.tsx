export function DocumentUploadDropzone({onFile,disabled}:{onFile:(file:File)=>void;disabled:boolean}){return <label className="flex cursor-pointer flex-col items-center rounded-xl border border-dashed border-border p-10 text-center hover:border-primary"><strong>Select a PDF for static inspection</strong><span className="mt-2 text-sm text-muted-foreground">PDF only · maximum 15 MB · original file is not retained</span><span className="mt-2 text-xs text-indigo-200">Links and embedded content are never opened.</span><input className="sr-only" type="file" accept="application/pdf,.pdf" disabled={disabled} onChange={e=>{const f=e.target.files?.[0];if(f)onFile(f);e.target.value=''}}/></label>}
