import { Link } from 'react-router-dom'; import type { SocEvent } from '../../../types'; import { EventTypeBadge } from './EventTypeBadge';
export function EvidenceEventTable({ events }: { events: SocEvent[] }) { return <div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b border-border text-muted-foreground"><tr><th className="p-3">Time</th><th>Type</th><th>Source</th><th>Identity</th><th>Outcome</th><th /></tr></thead><tbody className="divide-y divide-border">{events.map(event => <tr key={event.id}><td className="p-3">{new Date(event.event_time).toLocaleString()}</td><td><EventTypeBadge type={event.event_type} /></td><td>{event.source_ip || '—'}</td><td>{event.username || '—'}</td><td>{event.outcome || 'unknown'}</td><td><Link className="text-primary hover:underline" to={`/soc/events/${event.id}`}>Details</Link></td></tr>)}</tbody></table></div>; }
