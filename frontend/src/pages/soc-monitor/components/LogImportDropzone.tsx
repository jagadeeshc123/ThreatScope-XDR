export function LogImportDropzone({ onFile, disabled }: { onFile: (file: File) => void; disabled?: boolean }) { return <label className="flex cursor-pointer flex-col items-center rounded-lg border border-dashed border-border p-6 text-center hover:border-primary"><span className="font-medium">Choose a local log file</span><span className="mt-1 text-xs text-muted-foreground">JSONL, CSV, access/auth logs, or key=value text · 5 MB · 10,000 events</span><span className="mt-2 text-xs text-indigo-200">Parsed as text only; content is never executed.</span><input disabled={disabled} type="file" accept=".jsonl,.ndjson,.csv,.log,.txt" className="sr-only" onChange={event => { const file = event.target.files?.[0]; if (file) onFile(file); event.target.value = ''; }} /></label>; }
