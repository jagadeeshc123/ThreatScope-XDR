import type { SocEvent } from '../../../types';
export function EventTimeline({ events }: { events: SocEvent[] }) { return <ol className="space-y-3 border-l border-border pl-5">{events.map(event => <li key={event.id} className="relative"><span className="absolute -left-[25px] top-1.5 h-2 w-2 rounded-full bg-primary" /><p className="text-xs text-muted-foreground">{new Date(event.event_time).toLocaleString()}</p><p className="text-sm">{event.event_type.replaceAll('_', ' ')} · {event.outcome || 'unknown'} · {event.source_ip || event.username || 'unattributed'}</p></li>)}</ol>; }
