import type { SocDetectionRule } from '../../../types'; import { AlertSeverityBadge } from './AlertSeverityBadge'; import { ConfidenceBadge } from './ConfidenceBadge';
export function RuleCard({ rule, onToggle, onEdit, onDelete }: { rule: SocDetectionRule; onToggle: () => void; onEdit: () => void; onDelete: () => void }) { return <article className="rounded-lg border border-border bg-card p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-xs text-muted-foreground">{rule.rule_code}{rule.is_default ? ' · Default' : ' · Custom'}</p><h3 className="mt-1 font-semibold">{rule.name}</h3></div><input type="checkbox" checked={rule.enabled} onChange={onToggle} aria-label={`Enable ${rule.name}`} className="h-4 w-4 accent-primary" /></div><p className="mt-3 text-sm text-muted-foreground">{rule.description}</p><div className="mt-4 flex flex-wrap items-center gap-2"><AlertSeverityBadge severity={rule.severity} /><ConfidenceBadge confidence={rule.confidence} /><span className="text-xs">{rule.threshold} in {rule.window_seconds}s by {rule.group_by}</span></div><p className="mt-3 text-xs text-muted-foreground">{rule.remediation}</p><div className="mt-4 flex gap-2"><button onClick={onEdit} className="rounded-md border border-border px-3 py-2 text-xs font-semibold">Edit</button>{!rule.is_default && <button onClick={onDelete} className="rounded-md border border-red-400/40 px-3 py-2 text-xs font-semibold text-red-300">Delete</button>}</div></article>; }
