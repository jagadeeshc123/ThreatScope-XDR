import { useEffect, useState } from 'react';import { useParams } from 'react-router-dom';import { threatIntelApi } from '../../api/threatIntel';import { PageHeader,PageShell,SectionCard } from '../../components/ui';import { ImportSummaryCard } from './components';
export function ImportDetailsPage(){const {importId=''}=useParams();const [item,setItem]=useState<Record<string,unknown>|null>(null);useEffect(()=>{void threatIntelApi.get('imports',importId).then(setItem)},[importId]);return <PageShell><PageHeader title={`Import #${importId}`} subtitle="Persistent manifest only; the uploaded source file is not retained."/>{item?<><ImportSummaryCard item={item}/><SectionCard title="Safe manifest"><pre className="overflow-auto text-xs">{JSON.stringify(item,null,2)}</pre></SectionCard></>:<p>Loading import…</p>}</PageShell>}

