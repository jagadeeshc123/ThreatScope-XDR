import { useEffect,useState } from 'react';import { useParams } from 'react-router-dom';import { threatIntelApi } from '../../api/threatIntel';import { PageHeader,PageShell,SectionCard } from '../../components/ui';
export function ThreatIntelReportDetailsPage(){const {reportId=''}=useParams();const [item,setItem]=useState<Record<string,unknown>|null>(null);useEffect(()=>{void threatIntelApi.get('reports',reportId).then(setItem)},[reportId]);return <PageShell><PageHeader title={String(item?.title??`Report #${reportId}`)} subtitle="Sandboxed rendering of a static offline report."/>{item?<SectionCard><iframe title="Threat-intelligence report" sandbox="" srcDoc={String(item.html_content)} className="h-[70vh] w-full rounded bg-white"/></SectionCard>:<p>Loading report…</p>}</PageShell>}

