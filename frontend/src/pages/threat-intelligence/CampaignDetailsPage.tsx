import { useEffect,useState } from 'react';import { useParams } from 'react-router-dom';import { threatIntelApi } from '../../api/threatIntel';import { PageHeader,PageShell,SectionCard } from '../../components/ui';import { CampaignIndicatorGraph,ConfidenceMeter,IndicatorSeverityBadge } from './components';
export function CampaignDetailsPage(){const {campaignId=''}=useParams();const [item,setItem]=useState<Record<string,unknown>|null>(null);useEffect(()=>{void threatIntelApi.get('campaigns',campaignId).then(setItem)},[campaignId]);const indicators=(item?.indicators??[]) as Record<string,unknown>[];return <PageShell><PageHeader title={String(item?.name??`Campaign #${campaignId}`)} subtitle={String(item?.description??'Analyst-defined campaign context')}/>{item?<><SectionCard title="Campaign confidence"><div className="flex gap-4"><IndicatorSeverityBadge severity={String(item.severity)}/><ConfidenceMeter value={Number(item.confidence)}/></div></SectionCard><SectionCard title="Indicator graph"><CampaignIndicatorGraph indicators={indicators}/></SectionCard></>:<p>Loading campaign…</p>}</PageShell>}

