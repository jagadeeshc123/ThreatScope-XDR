import { DetectionLayout,DetectionTable,useDetectionPage } from './shared';
export function DetectionExecutionsPage(){const {data}=useDetectionPage('executions');return <DetectionLayout title="Historical Executions" subtitle="Bounded synchronous evaluation against stored data only."><DetectionTable title="Executions" items={(data?.items??[]) as Array<Record<string,unknown>>} detailBase="/detections/executions" columns={[["id","ID"],["status","Status"],["mode","Mode"],["records_scanned","Scanned"],["matches_found","Matches"],["suppressed_matches","Suppressed"]]}/></DetectionLayout>}
