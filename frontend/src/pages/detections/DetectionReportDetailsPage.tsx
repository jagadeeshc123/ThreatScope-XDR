import { useEffect,useState } from 'react';import { useParams } from 'react-router-dom';import { detectionsApi } from '../../api/detections';import { DetectionLayout } from './shared';
export function DetectionReportDetailsPage(){const {reportId}=useParams();const [item,setItem]=useState<Record<string,unknown>|null>(null);useEffect(()=>{void detectionsApi.get('reports',reportId!).then(setItem)},[reportId]);return <DetectionLayout title={String(item?.title??'Detection report')} subtitle="Sandboxed static report preview."><iframe title="Sandboxed detection report" sandbox="" srcDoc={String(item?.html_content??'')} className="min-h-[70vh] w-full rounded border bg-white"/></DetectionLayout>}
