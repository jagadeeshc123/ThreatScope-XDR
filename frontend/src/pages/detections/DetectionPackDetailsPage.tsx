import { useEffect,useState } from 'react';import { useParams } from 'react-router-dom';import { detectionsApi } from '../../api/detections';import { DetectionLayout,DetectionTable } from './shared';
export function DetectionPackDetailsPage(){const {packId}=useParams();const [pack,setPack]=useState<Record<string,unknown>|null>(null);useEffect(()=>{void detectionsApi.get('packs',packId!).then(setPack)},[packId]);return <DetectionLayout title={String(pack?.name??'Detection pack')} subtitle={String(pack?.description??'Loading local rule pack…')}><DetectionTable title="Pack rules" items={(pack?.rules??[]) as Array<Record<string,unknown>>} detailBase="/detections/rules" columns={[["title","Rule"],["severity","Severity"],["lifecycle_status","Status"],["quality_score","Quality"]]}/></DetectionLayout>}
