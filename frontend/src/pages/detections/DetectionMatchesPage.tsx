import { DetectionLayout,DetectionTable,useDetectionPage } from './shared';import { DetectionRiskBadge } from './components';
export function DetectionMatchesPage(){const {data}=useDetectionPage('matches');return <DetectionLayout title="Detection Matches" subtitle="Review deterministic rule outcomes without mutating their source records."><DetectionTable title="Stored matches" items={(data?.items??[]) as Array<Record<string,unknown>>} detailBase="/detections/matches" columns={[["id","ID"],["rule_id","Rule"],["risk_score","Risk",x=><DetectionRiskBadge score={Number(x.risk_score)}/>],["status","Disposition"],["source_module","Source"]]}/></DetectionLayout>}
