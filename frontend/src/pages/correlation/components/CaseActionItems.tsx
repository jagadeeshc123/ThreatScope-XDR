import{useState}from'react';import type{IncidentActionItem}from'../../../types';export function CaseActionItems({items=[],add}:{items?:IncidentActionItem[];add:(v:string)=>void}){const[v,setV]=useState('');return <div><div className="flex gap-2"><input value={v} onChange={e=>setV(e.target.value)} className="flex-1 rounded border border-input bg-background px-3"/><button onClick={()=>{add(v);setV('')}}>Add task</button></div>{items.map(x=><p key={x.id} className="mt-2">{x.title} · {x.status}</p>)}</div>}
