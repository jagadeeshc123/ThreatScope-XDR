export function RelationshipSummary({observations,modules,matches,cases}:{observations:number;modules:number;matches:number;cases:number}){return <div className="grid grid-cols-4 gap-3 text-center">{[[observations,'Observations'],[modules,'Modules'],[matches,'Matches'],[cases,'Cases']].map(([v,l])=><div key={String(l)} className="rounded bg-muted p-3"><b>{v}</b><small className="block">{l}</small></div>)}</div>}
