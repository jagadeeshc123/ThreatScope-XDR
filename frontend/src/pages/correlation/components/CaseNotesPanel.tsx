import{useState}from'react';import type{IncidentNote}from'../../../types';export function CaseNotesPanel({items=[],add}:{items?:IncidentNote[];add:(v:string)=>void}){const[v,setV]=useState('');return <div><div className="flex gap-2"><input value={v} onChange={e=>setV(e.target.value)} className="flex-1 rounded border border-input bg-background px-3"/><button onClick={()=>{add(v);setV('')}}>Add note</button></div>{items.map(x=><p key={x.id} className="mt-2">{x.note_text}</p>)}</div>}
