import{useState}from'react';export function UrlAnalysisForm({submit,busy}:{submit:(v:string)=>void;busy:boolean}){const[v,setV]=useState('');return <form onSubmit={e=>{e.preventDefault();submit(v)}} className="space-y-3"><input required value={v} onChange={e=>setV(e.target.value)} placeholder="https://example.com/path?token=fake" className="h-10 w-full rounded border border-input bg-background px-3"/><p className="text-sm text-muted-foreground">Lexical analysis only. No request, DNS lookup, redirect, or reputation lookup is performed.</p><button disabled={busy} className="rounded bg-primary px-4 py-2 font-semibold text-primary-foreground">{busy?'Analyzing…':'Analyze URL text'}</button></form>}
