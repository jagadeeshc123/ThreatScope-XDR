import{useState}from'react';export function AnalystDispositionPanel({current,notes,save}:{current:string;notes:string|null;save:(d:string,n:string)=>void}){const[d,setD]=useState(current),[n,setN]=useState(notes||'');return <div className="grid gap-3"><select value={d} onChange={e=>setD(e.target.value)} className="h-10 rounded border border-input bg-background px-3">{['unreviewed','legitimate','suspicious','phishing','false_positive'].map(x=><option key={x}>{x}</option>)}</select><textarea value={n} onChange={e=>setN(e.target.value)} placeholder="Redacted analyst notes" className="rounded border border-input bg-background p-3"/><button onClick={()=>save(d,n)} className="rounded bg-primary px-4 py-2">Save disposition</button></div>}
