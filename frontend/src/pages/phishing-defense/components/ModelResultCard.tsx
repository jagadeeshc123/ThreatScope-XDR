export function ModelResultCard({probability,label}:{probability:number|null;label:string|null}){return <div className="rounded border border-border p-4"><b>Local demonstration classifier</b><p>{probability===null?'Unavailable':`${(probability*100).toFixed(1)}%`} — {label}</p><p className="text-xs text-muted-foreground">Bundled synthetic examples; not validated for production email-security decisions.</p></div>}
