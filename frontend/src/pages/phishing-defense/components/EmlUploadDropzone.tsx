export function EmlUploadDropzone({submit,busy}:{submit:(f:File)=>void;busy:boolean}){return <div className="space-y-3 rounded-lg border border-dashed border-border p-6"><p className="text-sm text-muted-foreground">Upload one inert .eml, maximum 5 MB. It is parsed in memory and the original is not retained.</p><input type="file" accept=".eml,message/rfc822" disabled={busy} onChange={e=>{const f=e.target.files?.[0];if(f)submit(f)}}/></div>}
