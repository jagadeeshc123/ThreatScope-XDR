import type{PhishingFinding}from'../../../types';export function PhishingFindingCard({item}:{item:PhishingFinding}){return <div className="rounded-lg border border-border p-4"><div className="flex justify-between"><b>{item.rule_code} — {item.title}</b><span className="uppercase text-xs">{item.severity}</span></div><p className="mt-2 text-sm text-muted-foreground">{item.evidence_summary}</p><p className="mt-2 text-sm">{item.remediation}</p></div>}
