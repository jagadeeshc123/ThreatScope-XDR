import { Link,useLocation } from 'react-router-dom';import type { SafeAccount } from '../../api/access';import { RegistrationStatusBadge } from './components/RegistrationStatusBadge';
export function AccountRejectedPage(){const account=(useLocation().state as {account?:SafeAccount}|null)?.account;return <main className="grid min-h-screen place-items-center bg-background p-5"><div className="w-full max-w-md rounded-xl border bg-card p-7 text-center"><RegistrationStatusBadge status="rejected"/><h1 className="mt-5 text-2xl font-semibold">Account not approved</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">This local registration was not approved. Contact your ThreatScope administrator for assistance.</p>{account?.rejection_reason&&<p className="mt-4 rounded border bg-background p-3 text-sm">{account.rejection_reason}</p>}<Link to="/login" className="mt-6 inline-block rounded bg-primary px-4 py-2 font-semibold text-primary-foreground">Return to Sign In</Link></div></main>}
