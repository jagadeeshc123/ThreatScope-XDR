export interface SessionRow { id: number; current?: boolean; created_at: string; last_seen_at: string; expires_at: string; revoked_at?: string | null; user_agent_summary?: string | null; client_ip_hash?: string | null }
export function SessionTable({ sessions, onRevoke }: { sessions: SessionRow[]; onRevoke: (id: number) => void }) { return <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="text-muted-foreground"><tr><th className="p-3">Session</th><th className="p-3">Created</th><th className="p-3">Last active</th><th className="p-3">Expires</th><th className="p-3">Client</th><th className="p-3">Action</th></tr></thead><tbody>{sessions.map(item => <tr key={item.id} className="border-t border-border"><td className="p-3">#{item.id} {item.current && <strong className="text-primary">Current</strong>}</td><td className="p-3">{new Date(item.created_at).toLocaleString()}</td><td className="p-3">{new Date(item.last_seen_at).toLocaleString()}</td><td className="p-3">{new Date(item.expires_at).toLocaleString()}</td><td className="p-3"><span className="block max-w-xs truncate">{item.user_agent_summary || 'Unknown'}</span><span className="text-xs text-muted-foreground">{item.client_ip_hash}</span></td><td className="p-3"><button disabled={!!item.revoked_at} onClick={() => onRevoke(item.id)} className="text-red-300 disabled:opacity-40">{item.revoked_at ? 'Revoked' : 'Revoke'}</button></td></tr>)}</tbody></table></div>; }
