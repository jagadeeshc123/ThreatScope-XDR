export function RecoveryCodesPanel({ codes, onAcknowledge }: { codes: string[]; onAcknowledge?: () => void }) { return <div className="rounded-lg border border-amber-500/40 bg-amber-500/10 p-4"><h3 className="font-semibold text-amber-100">Recovery codes — shown once</h3><p className="mt-1 text-sm text-muted-foreground">Store these codes offline. Each code can be used only once.</p><div className="mt-3 grid grid-cols-2 gap-2 font-mono text-sm">{codes.map(code => <code key={code}>{code}</code>)}</div>{onAcknowledge && <button onClick={onAcknowledge} className="mt-4 rounded-md bg-amber-500 px-3 py-2 text-sm font-semibold text-black">I stored these codes</button>}</div>; }
