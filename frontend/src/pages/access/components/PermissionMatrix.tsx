export interface PermissionItem { permission_key: string; name: string; category: string }
export function PermissionMatrix({ permissions, selected, onChange, disabled = false }: { permissions: PermissionItem[]; selected: string[]; onChange: (keys: string[]) => void; disabled?: boolean }) { const groups = permissions.reduce<Record<string, PermissionItem[]>>((result, item) => { (result[item.category] ||= []).push(item); return result; }, {}); return <div className="space-y-5">{Object.entries(groups).map(([category, items]) => <fieldset key={category} className="rounded-md border border-border p-4"><legend className="px-2 text-sm font-semibold">{category}</legend><div className="grid gap-2 md:grid-cols-2">{items.map(item => <label key={item.permission_key} className="flex gap-2 text-sm"><input type="checkbox" disabled={disabled} checked={selected.includes(item.permission_key)} onChange={event => onChange(event.target.checked ? [...selected, item.permission_key] : selected.filter(key => key !== item.permission_key))}/><span>{item.name}<small className="block text-muted-foreground">{item.permission_key}</small></span></label>)}</div></fieldset>)}</div>; }
