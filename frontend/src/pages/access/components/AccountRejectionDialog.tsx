import { useState } from 'react';
export function AccountRejectionDialog({open,onClose,onReject}:{open:boolean;onClose:()=>void;onReject:(reason:string)=>Promise<void>}){const [reason,setReason]=useState('');const [busy,setBusy]=useState(false);if(!open)return null;return <div className="fixed inset-0 z-[90] grid place-items-center bg-black/70 p-4" role="dialog" aria-modal="true" aria-label="Reject registration"><div className="w-full max-w-md rounded-lg border bg-card p-6"><h2 className="text-lg font-semibold">Reject registration</h2><label className="mt-4 block text-sm">Reason<textarea required minLength={3} maxLength={500} value={reason} onChange={e=>setReason(e.target.value)} className="mt-2 min-h-28 w-full rounded border bg-background p-3"/></label><div className="mt-5 flex gap-3"><button disabled={busy||reason.trim().length<3} onClick={()=>{setBusy(true);void onReject(reason).finally(()=>setBusy(false))}} className="rounded bg-red-600 px-4 py-2 font-semibold text-white disabled:opacity-50">Reject</button><button onClick={onClose}>Cancel</button></div></div></div>}
