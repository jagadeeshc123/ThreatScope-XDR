export function EmailField({value,onChange,label='Email address'}:{value:string;onChange:(value:string)=>void;label?:string}){return <label className="block text-sm font-medium">{label}<input type="email" autoComplete="email" required maxLength={254} value={value} onChange={event=>onChange(event.target.value)} className="mt-2 block w-full rounded-md border border-input bg-background px-3 py-2 outline-none focus:ring-2 focus:ring-primary"/></label>}
