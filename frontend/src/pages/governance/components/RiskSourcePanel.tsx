import type{GovernanceRiskSource}from'../../../types';import{SourceModuleBadge}from'./SourceModuleBadge';export function RiskSourcePanel({items=[]}:{items?:GovernanceRiskSource[]}){return <div className="space-y-2">{items.length?items.map(x=><div key={x.id} className="rounded border border-border p-3"><SourceModuleBadge value={x.source_module}/> <span className="ml-2 font-medium">{x.title_snapshot}</span><p className="mt-2 text-sm text-muted-foreground">{x.evidence_snapshot}</p>{x.source_internal_route&&<a href={x.source_internal_route} className="text-xs text-primary">Open internal source</a>}</div>):<p className="text-sm text-muted-foreground">No synchronized source evidence.</p>}</div>}
