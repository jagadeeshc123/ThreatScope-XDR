import type{GovernanceControlMapping}from'../../../types';import{MappingStatusBadge}from'./MappingStatusBadge';export function ControlMappingPanel({items=[]}:{items?:GovernanceControlMapping[]}){return <div className="space-y-2">{items.length?items.map(x=><div key={x.id} className="rounded border p-3"><MappingStatusBadge value={x.mapping_status}/> <span className="ml-2">{x.control?.control_key} · {x.control?.title}</span><p className="mt-2 text-sm text-muted-foreground">{x.rationale}</p></div>):<p className="text-sm text-muted-foreground">No control mappings.</p>}</div>}
