import{LineChart,Line,ResponsiveContainer,XAxis,YAxis,Tooltip}from'recharts';import type{GovernanceSnapshot}from'../../../types';export function RiskTrendChart({items}:{items:GovernanceSnapshot[]}){const data=items.map(x=>({date:x.metric_date.slice(0,10),risk:Number(x.metrics?.residual_risk_total||0)}));return <div className="h-56">{data.length?<ResponsiveContainer width="100%" height="100%"><LineChart data={data}><XAxis dataKey="date"/><YAxis/><Tooltip/><Line type="monotone" dataKey="risk" stroke="hsl(var(--primary))"/></LineChart></ResponsiveContainer>:<p className="text-sm text-muted-foreground">Capture snapshots to build a local trend.</p>}</div>}
