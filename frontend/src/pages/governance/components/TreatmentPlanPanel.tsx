import type{RiskTreatmentPlan}from'../../../types';export function TreatmentPlanPanel({items=[]}:{items?:RiskTreatmentPlan[]}){return <div className="space-y-2"><p className="text-xs text-amber-200">Treatment records are workflow plans only and do not execute technical remediation.</p>{items.map(x=><div key={x.id} className="rounded border p-3"><b>{x.title}</b> · {x.strategy} · {x.status}<p className="text-sm text-muted-foreground">Expected residual values are suggestions; actual risk is unchanged.</p></div>)}</div>}
