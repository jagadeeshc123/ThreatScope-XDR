# CSRF mutation inventory

Generated from the registered FastAPI route/dependency graph. GET/HEAD/OPTIONS routes are excluded. Central coverage validates CSRF only for authenticated browser mutations after session and permission checks; the four intentional non-session exceptions use the controls stated below.

Total mutation routes: 355. Central coverage: 300. Explicit authenticated CSRF coverage: 51. Intentional non-session exceptions: 4. Unclassified: 0.

| Method | Route | Coverage |
| --- | --- | --- |
| POST | `/api/admin/registrations/{user_id}/approve` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/admin/registrations/{user_id}/reject` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/admin/registrations/{user_id}/reopen` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/admin/roles` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| DELETE | `/api/admin/roles/{role_id}` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| PATCH | `/api/admin/roles/{role_id}` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| PUT | `/api/admin/roles/{role_id}/permissions` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/admin/users` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| PATCH | `/api/admin/users/{user_id}` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/admin/users/{user_id}/disable` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/admin/users/{user_id}/enable` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/admin/users/{user_id}/reset-mfa` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/admin/users/{user_id}/reset-password` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/admin/users/{user_id}/revoke-all-sessions` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| PUT | `/api/admin/users/{user_id}/roles` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| DELETE | `/api/admin/users/{user_id}/sessions/{session_id}` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/admin/users/{user_id}/unlock` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/analytics/anomalies/{anomaly_id}/acknowledge` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/anomalies/{anomaly_id}/assign` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/anomalies/{anomaly_id}/case-proposal` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/anomalies/{anomaly_id}/confirm` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/anomalies/{anomaly_id}/dismiss` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/anomalies/{anomaly_id}/feedback` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/anomalies/{anomaly_id}/investigate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/anomalies/{anomaly_id}/link-case` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/anomalies/{anomaly_id}/resolve` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/backtests` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/baselines/build` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/detectors` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/analytics/detectors/{detector_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/detectors/{detector_id}/activate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/detectors/{detector_id}/disable` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/detectors/{detector_id}/retire` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/detectors/{detector_id}/rollback` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/detectors/{detector_id}/validate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/detectors/{detector_id}/versions` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/drift/evaluate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/drift/{drift_id}/acknowledge` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/jobs` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/jobs/{job_id}/cancel` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/process-due` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/suppressions` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/analytics/suppressions/{suppression_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/analytics/suppressions/{suppression_id}/disable` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/api-security/assessments` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/api-security/assessments/{assessment_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/api-security/assessments/{assessment_id}/analyze` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/api-security/assessments/{assessment_id}/authorization-matrix` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/api-security/assessments/{assessment_id}/authorization-review/generate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/api-security/assessments/{assessment_id}/business-flows` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/api-security/assessments/{assessment_id}/identities` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/api-security/assessments/{assessment_id}/import/openapi` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/api-security/assessments/{assessment_id}/import/postman` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/api-security/assessments/{assessment_id}/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/api-security/assessments/{assessment_id}/roles` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/api-security/authorization-matrix/{entry_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/api-security/authorization-matrix/{entry_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/api-security/authorization-reviews/{review_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/api-security/business-flow-risks/{risk_id}` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/api-security/business-flow-steps/{step_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/api-security/business-flow-steps/{step_id}` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/api-security/business-flows/{flow_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/api-security/business-flows/{flow_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/api-security/business-flows/{flow_id}/analyze` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/api-security/business-flows/{flow_id}/steps` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/api-security/identities/{identity_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/api-security/identities/{identity_id}` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/api-security/jwt/analyses/{analysis_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/api-security/jwt/analyze` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/api-security/roles/{role_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/api-security/roles/{role_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/auth/login` | Pre-authentication login; rate limit/account lockout applies |
| POST | `/api/auth/logout` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/auth/logout-all` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/auth/mfa/cancel` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/auth/mfa/confirm` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/auth/mfa/disable` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/auth/mfa/enroll` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/auth/mfa/recovery/regenerate` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/auth/mfa/verify-login` | Pre-session MFA challenge; bounded signed challenge and failure controls apply |
| POST | `/api/auth/password/change` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/auth/register` | Pre-authentication registration; runtime registration policy/rate controls apply |
| DELETE | `/api/auth/sessions/{session_id}` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/correlation/actions/check-overdue` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/correlation/cases` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/correlation/cases/from-match/{match_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/correlation/cases/from-source` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/correlation/cases/{case_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/correlation/cases/{case_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/correlation/cases/{case_id}/actions` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/correlation/cases/{case_id}/actions/{action_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/correlation/cases/{case_id}/actions/{action_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/correlation/cases/{case_id}/evidence` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/correlation/cases/{case_id}/evidence/{evidence_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/correlation/cases/{case_id}/notes` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/correlation/cases/{case_id}/notes/{note_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/correlation/cases/{case_id}/notes/{note_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/correlation/cases/{case_id}/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/correlation/entities/sync` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/correlation/matches/run` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/correlation/matches/{match_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/correlation/matches/{match_id}/create-case` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/correlation/rules/{rule_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/correlation/rules/{rule_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/executions` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/executions/{execution_id}/cancel` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/imports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/imports/validate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/matches/{match_id}/create-alert` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/matches/{match_id}/escalate-case` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/matches/{match_id}/review` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/packs` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/detections/packs/{pack_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/packs/{pack_id}/rules` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/detections/packs/{pack_id}/rules/{rule_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/rules` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/detections/rules/{rule_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/rules/{rule_id}/activate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/rules/{rule_id}/archive` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/rules/{rule_id}/clone` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/rules/{rule_id}/disable` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/rules/{rule_id}/rollback` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/rules/{rule_id}/tests` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/rules/{rule_id}/tests/run` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/detections/rules/{rule_id}/tests/{test_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/detections/rules/{rule_id}/tests/{test_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/rules/{rule_id}/validate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/detections/suppressions` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/detections/suppressions/{suppression_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/detections/suppressions/{suppression_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/document-threats/analyses` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/document-threats/analyses/{analysis_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/document-threats/analyses/{analysis_id}/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/checks/overdue` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/governance/controls/{control_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/evidence-packages` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/governance/evidence-packages/{package_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/governance/evidence-packages/{package_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/evidence-packages/{package_id}/items` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/governance/evidence-packages/{package_id}/items/{item_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/evidence-packages/{package_id}/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/exceptions/check-expired` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/governance/exceptions/{exception_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/frameworks/seed` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/governance/frameworks/{framework_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/governance/frameworks/{framework_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/frameworks/{framework_id}/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/mappings/generate` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/governance/mappings/{mapping_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/governance/mappings/{mapping_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/reports/executive` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/reports/risk-register` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/reviews` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/governance/reviews/{review_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/governance/reviews/{review_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/reviews/{review_id}/complete` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/reviews/{review_id}/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/risks` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/risks/sync` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/governance/risks/{risk_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/governance/risks/{risk_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/risks/{risk_id}/exceptions` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/risks/{risk_id}/treatments` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/governance/risks/{risk_id}/treatments/{treatment_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/governance/risks/{risk_id}/treatments/{treatment_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/governance/snapshots/capture` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/connectors` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/integrations/connectors/{connector_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/connectors/{connector_id}/activate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/connectors/{connector_id}/archive` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/integrations/connectors/{connector_id}/credentials` | Central authenticated session + permission + CSRF + audit |
| PUT | `/api/integrations/connectors/{connector_id}/credentials` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/connectors/{connector_id}/credentials/rotate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/connectors/{connector_id}/disable` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/connectors/{connector_id}/move-to-testing` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/integrations/connectors/{connector_id}/network-policy` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/connectors/{connector_id}/reset-circuit` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/connectors/{connector_id}/send-test` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/connectors/{connector_id}/taxii/pull` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/connectors/{connector_id}/test` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/connectors/{connector_id}/validate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/dead-letters/{item_id}/cancel` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/dead-letters/{item_id}/replay` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/deliveries` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/deliveries/{item_id}/cancel` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/deliveries/{item_id}/retry` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/health-checks/run` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/inbound-endpoints` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/integrations/inbound-endpoints/{item_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/inbound-endpoints/{item_id}/disable` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/inbound-endpoints/{item_id}/rotate-secret` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/inbound-events/{item_id}/promote` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/inbound-events/{item_id}/reject` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/inbound/{endpoint_uuid}` | No browser session; endpoint secret signature, replay, size, rate, and quarantine controls apply |
| POST | `/api/integrations/mappings` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/integrations/mappings/{item_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/integrations/mappings/{item_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/mappings/{item_id}/preview` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/mappings/{item_id}/validate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/process-due` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/stix/import/preview` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/stix/import/promote` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/integrations/subscriptions` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/integrations/subscriptions/{item_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/integrations/subscriptions/{item_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/notifications/mark-all-read` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/notifications/{notification_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/notifications/{notification_id}/read` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/operations/backups/database` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| PUT | `/api/operations/backups/retention` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/backups/retention/apply` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/backups/retention/preview` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| DELETE | `/api/operations/backups/{backup_id}` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/backups/{backup_id}/verify` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/configuration/validate` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/demo/reseed` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/demo/reset` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/demo/seed` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/exports` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| DELETE | `/api/operations/exports/{export_id}` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/exports/{export_id}/verify` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/imports/validate` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/inventory/generate` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/jobs/{job_id}/cancel` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/production/preflight` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/releases/build` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| DELETE | `/api/operations/releases/{release_id}` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/restores/validate` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/restores/{restore_id}/execute` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/retention/apply` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| PUT | `/api/operations/retention/policies/{policy_id}` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/operations/retention/preview` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| POST | `/api/phishing-defense/analyses/email-text` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/phishing-defense/analyses/eml` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/phishing-defense/analyses/url` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/phishing-defense/analyses/{analysis_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/phishing-defense/analyses/{analysis_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/phishing-defense/analyses/{analysis_id}/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/phishing-defense/watchlist` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/phishing-defense/watchlist/{entry_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/phishing-defense/watchlist/{entry_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/profile/` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/reports/generate/{scan_id}` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/reports/{report_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/scans/start` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/scans/{scan_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/security-audit/verify-integrity` | Explicit authenticated session + CSRF (permission dependency is route-owned) |
| PATCH | `/api/settings/` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/settings/reset` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/soar/action-policies/{action_key}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/analyst-inputs/{input_id}/cancel` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/analyst-inputs/{input_id}/submit` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/approvals/{approval_id}/approve` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/approvals/{approval_id}/cancel` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/approvals/{approval_id}/reject` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/executions` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/executions/process-due` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/executions/{execution_id}/cancel` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/executions/{execution_id}/request-rollback` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/executions/{execution_id}/resume` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/executions/{execution_id}/retry` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/playbooks` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/soar/playbooks/{playbook_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/playbooks/{playbook_id}/activate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/playbooks/{playbook_id}/archive` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/playbooks/{playbook_id}/clone` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/playbooks/{playbook_id}/disable` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/playbooks/{playbook_id}/move-to-testing` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/playbooks/{playbook_id}/rollback-version` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/playbooks/{playbook_id}/validate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/rollbacks/{rollback_id}/approve` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/rollbacks/{rollback_id}/cancel` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/rollbacks/{rollback_id}/execute` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/templates/{template_id}/clone` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/triggers` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soar/triggers/evaluate` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/soar/triggers/{trigger_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/soar/triggers/{trigger_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/soc/alerts/{alert_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soc/blocklist` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/soc/blocklist/{entry_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/soc/blocklist/{entry_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soc/detections/run` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soc/enrichment` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soc/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soc/rules` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/soc/rules/{rule_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/soc/rules/{rule_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soc/simulator/generate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soc/sources` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/soc/sources/{source_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/soc/sources/{source_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/soc/sources/{source_id}/imports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/targets/` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/targets/{target_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/targets/{target_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/threat-intel/campaigns` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/threat-intel/campaigns/{campaign_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/threat-intel/correlation/run` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/threat-intel/imports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/threat-intel/indicators` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/threat-intel/indicators/{indicator_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/threat-intel/indicators/{indicator_id}/restore` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/threat-intel/indicators/{indicator_id}/revoke` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/threat-intel/matches/{match_id}/escalate` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/threat-intel/matches/{match_id}/review` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/threat-intel/relationships` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/threat-intel/relationships/{relationship_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/threat-intel/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/threat-intel/sources` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/threat-intel/sources/{source_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/threat-intel/watchlists` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/threat-intel/watchlists/{watchlist_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/threat-intel/watchlists/{watchlist_id}/entries` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/threat-intel/watchlists/{watchlist_id}/entries/{indicator_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/assets` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/assets/synchronize` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/vulnerability-management/assets/{asset_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/assets/{asset_id}/aliases` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/vulnerability-management/assets/{asset_id}/aliases/{alias_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/assets/{asset_id}/relationships` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/vulnerability-management/assets/{asset_id}/relationships/{relationship_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/ingestion/run` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/vulnerability-management/plans/{plan_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/plans/{plan_id}/approve` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/plans/{plan_id}/complete` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/plans/{plan_id}/tasks` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/remediation-templates` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/vulnerability-management/remediation-templates/{template_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/reports` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/risk-acceptances/{acceptance_id}/approve` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/risk-acceptances/{acceptance_id}/reject` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/risk-acceptances/{acceptance_id}/revoke` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/sla-policies` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/sla-policies/recalculate` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/vulnerability-management/sla-policies/{policy_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/sla-policies/{policy_id}/disable` | Central authenticated session + permission + CSRF + audit |
| DELETE | `/api/vulnerability-management/tasks/{task_id}` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/vulnerability-management/tasks/{task_id}` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/verifications/{verification_id}/cancel` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/verifications/{verification_id}/complete` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/verifications/{verification_id}/start` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/vulnerabilities/{vulnerability_id}/assign` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/vulnerabilities/{vulnerability_id}/comments` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/vulnerabilities/{vulnerability_id}/merge` | Central authenticated session + permission + CSRF + audit |
| PATCH | `/api/vulnerability-management/vulnerabilities/{vulnerability_id}/metadata` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/vulnerabilities/{vulnerability_id}/plans` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/vulnerabilities/{vulnerability_id}/risk-acceptances` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/vulnerabilities/{vulnerability_id}/split` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/vulnerabilities/{vulnerability_id}/transition` | Central authenticated session + permission + CSRF + audit |
| POST | `/api/vulnerability-management/vulnerabilities/{vulnerability_id}/verifications` | Central authenticated session + permission + CSRF + audit |
